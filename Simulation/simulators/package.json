{
  "name": "infflux-simulators",
  "version": "0.1.0",
  "private": true,
  "description": "Simulateurs (marchand, entrepôt, transporteur) de la plateforme logistique Infflux. Communiquent avec le backend uniquement via HTTP.",
  "scripts": {
    "merchant": "tsx merchant/index.ts",
    "spawn-merchant": "tsx merchant/spawn.ts",
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^22.13.10",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.9",
    "tsx": "^4.19.2",
    "typescript": "^5.8.2"
  }
}
